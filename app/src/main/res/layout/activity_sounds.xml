<?xml version="1.0" encoding="utf-8"?>
<FrameLayout android:id="@+id/sounds_background_frame_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ImageView
        android:id="@+id/sounds_activity_background_image"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:src="@drawable/congaree"
        android:contentDescription="sound_activity_background"
        android:scaleType="centerCrop"
        android:alpha="0.7" />

    <LinearLayout
        android:id="@+id/linear_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            android:id="@+id/tool_bar"
            layout="@layout/tool_bar">
        </include>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/sounds_list_container">

        </FrameLayout>
    </LinearLayout>
</FrameLayout>